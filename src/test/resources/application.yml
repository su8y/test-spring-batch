spring:
  config:
    activate:
      on-profile: test
  datasource:
    hikari:
      #      jdbc-url: jdbc:h2:mem:jdbctest;MODE=MySQL;
      jdbc-url: jdbc:mysql://localhost:3306/test?rewriteBatchedStatements=true&profileSQL=true&logger=Slf4JLogger&maxQuerySizeToLog=999999
      driver-class-name: com.mysql.jdbc.Driver
      username: root
      password: 1234
  sql:
    init:
      mode: always
  batch:
    jdbc:
      initialize-schema: always
  jpa:
    properties:
      hibernate:
        jdbc:
          batch_size: 10
        show_sql: true
        order_updates: true
        order_inserts: true
    hibernate:
      ddl-auto: create-drop
  output:
    ansi:
      enabled: always
---
spring:
  config:
    activate:
      on-profile: hello
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:testdb
      username: sa
      password:

#logging:
#  level:
#    org:
#      springframework:
#        jdbc: trace
