Spring Batch 

## 시작 
```shell
$ ./gradlew bootRun

# 만약 permission denined 아래 커맨드 실행
$ chmod 777 ./gradlew
```

**JOB 실행 API**
* GET: `/job/step1?name={중복을 허용하지 않는 이름}`
* GET: `/jobs/{JobParameter에 들어가는 이름}`

